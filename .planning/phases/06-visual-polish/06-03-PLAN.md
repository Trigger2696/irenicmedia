---
phase: 06-visual-polish
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - irenic-media-new/app/globals.css
  - irenic-media-new/components/Hero/Hero.tsx
autonomous: true

must_haves:
  truths:
    - "CSS custom properties match marko template exactly"
    - "Animation timing variables are available for consistent animations"
    - "Extended font sizes (11xl-17xl) available for hero headlines"
    - "Hero section has scroll animation matching marko template style"
    - "Reduced motion preferences are respected"
  artifacts:
    - path: "irenic-media-new/app/globals.css"
      provides: "Complete marko template CSS variables"
      contains: "--animation-normal"
    - path: "irenic-media-new/components/Hero/Hero.tsx"
      provides: "Hero with scroll animation"
      contains: "AnimateOnScroll"
  key_links:
    - from: "irenic-media-new/components/Hero/Hero.tsx"
      to: "irenic-media-new/components/AnimateOnScroll.tsx"
      via: "import AnimateOnScroll"
      pattern: "import.*AnimateOnScroll"
---

<objective>
Complete CSS variable audit and add Hero animation

Purpose: Ensure all CSS custom properties match marko template exactly and apply scroll animation to Hero section for complete visual parity.

Output: Complete CSS variables in globals.css, Hero section animates on scroll, reduced motion is respected.
</objective>

<execution_context>
@C:\Users\Raj Shah\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Raj Shah\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-visual-polish/06-RESEARCH.md
@.planning/phases/06-visual-polish/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete CSS custom properties audit</name>
  <files>irenic-media-new/app/globals.css</files>
  <action>
Add missing CSS custom properties to match marko template exactly.

**Add to :root section (after existing font-size-10xl):**
```css
/* Extended Font Size Variables (marko template) */
--font-size-11xl: 46px;
--font-size-12xl: 50px;
--font-size-13xl: 56px;
--font-size-14xl: 64px;
--font-size-15xl: 100px;
--font-size-16xl: 130px;
--font-size-17xl: 160px;

/* Animation Timing Variables (marko template) */
--animation-fast: 0.75s;
--animation-normal: 1.25s;
--animation-slow: 2s;

/* Spacing Variables */
--spacing-xs: 0.5rem;
--spacing-sm: 1rem;
--spacing-md: 1.5rem;
--spacing-lg: 2rem;
--spacing-xl: 3rem;
--spacing-2xl: 4rem;
--spacing-3xl: 6rem;
```

**Add reduced motion support (after .light theme block):**
```css
/* Reduced Motion Support */
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

**Verify existing variables match marko template:**
- --primary: #D1D1D1 (dark) / #1F1F1F (light) ✓
- --secondary: #040404 (dark) / #FFFAFA (light) ✓
- --accent-color: #C82AEF ✓
- --global-border-radius: 25px ✓
- Box shadow variables ✓
  </action>
  <verify>
Open globals.css and confirm:
- Extended font sizes 11xl-17xl exist
- Animation timing variables exist
- Reduced motion media query exists
CSS compiles without errors (dev server runs).
  </verify>
  <done>globals.css contains complete marko template CSS variables including extended font sizes, animation timing, and reduced motion support.</done>
</task>

<task type="auto">
  <name>Task 2: Add scroll animation to Hero section</name>
  <files>irenic-media-new/components/Hero/Hero.tsx</files>
  <action>
Currently Hero.tsx has inline AnimateOnScroll for stats but not for the main content. Add scroll animation to Hero headline and CTAs to match marko template.

**Changes to make:**

1. Import shared AnimateOnScroll component:
   ```typescript
   import AnimateOnScroll from '@/components/AnimateOnScroll'
   ```

2. Remove inline AnimateOnScroll function if present (likely exists for stats animation)

3. Wrap hero content sections with AnimateOnScroll:

For the main headline/subheadline/CTAs container, wrap with:
```tsx
<AnimateOnScroll animation="fadeInUp" delay={0.2}>
  {/* Hero badge */}
  <div className="flex items-center gap-2 mb-6">
    ...
  </div>

  {/* Headline */}
  <h1 className="...">
    ...
  </h1>

  {/* Subheadline */}
  <p className="...">
    ...
  </p>

  {/* CTAs */}
  <div className="flex flex-wrap gap-4 justify-center lg:justify-start">
    ...
  </div>
</AnimateOnScroll>
```

For the stats section, wrap with staggered animations:
```tsx
<div className="flex flex-wrap gap-8 justify-center lg:justify-start">
  {stats.map((stat, index) => (
    <AnimateOnScroll key={stat.label} animation="fadeInUp" delay={0.4 + index * 0.1}>
      <StatCounter ... />
    </AnimateOnScroll>
  ))}
</div>
```

**Important:** Hero has YouTube background that should NOT be animated - only the text content overlays should animate.

4. If Hero uses inline motion.div for any animations, replace with AnimateOnScroll for consistency.
  </action>
  <verify>
Run dev server, load page.
Hero headline and stats should fade in on initial load.
YouTube video background should start immediately (not delayed by animation).
Scroll down and back up - animations should NOT replay (triggerOnce).
  </verify>
  <done>Hero section uses AnimateOnScroll for headline, CTAs, and stats. Animation timing matches other sections (600ms, easeOut).</done>
</task>

<task type="auto">
  <name>Task 3: Final visual verification and cleanup</name>
  <files>
    irenic-media-new/components/Hero/Hero.tsx
    irenic-media-new/app/page.tsx
  </files>
  <action>
Perform final verification that all visual polish requirements are met:

1. **Verify all sections have scroll animations:**
   - Hero: fadeInUp on headline, staggered stats
   - Services: fadeInUp with 0.1s stagger on cards
   - About: fadeInLeft/fadeInRight for columns
   - WhyUs: fadeInUp with stagger on cards
   - Testimonials: fadeInUp with stagger
   - CaseStudies: fadeInUp with stagger
   - Pricing: fadeInUp with stagger
   - Contact: fadeInLeft/fadeIn for columns

2. **Check animation timing consistency:**
   All should use 600ms duration, easeOut timing, 15% threshold.

3. **Verify reduced motion works:**
   Open browser devtools, toggle "prefers-reduced-motion: reduce" in Rendering panel.
   Animations should be instant (not visible) when enabled.

4. **If any section is missing animations or has inconsistent timing, fix it.**

5. **Check for any remaining inline animation code that should use shared component.**

No new files to create - this is verification and minor cleanup only.
  </action>
  <verify>
Run these verification commands:
1. Grep for AnimateOnScroll usage: `grep -r "AnimateOnScroll" irenic-media-new/components/ --include="*.tsx" | wc -l` (expect 8+ matches)
2. Grep for inline motion.div: `grep -r "motion\.div" irenic-media-new/components/ --include="*.tsx"` (should be minimal or none outside AnimateOnScroll.tsx)
3. Check for duplicate AnimateOnScroll definitions: `grep -l "function AnimateOnScroll\|const AnimateOnScroll" irenic-media-new/components/**/*.tsx` (should only be AnimateOnScroll.tsx)
4. Dev server test: `cd irenic-media-new && npm run dev` - scroll through all 8 sections, verify animations trigger
5. Browser devtools: Toggle "prefers-reduced-motion: reduce" in Rendering panel, confirm animations disable
  </verify>
  <done>All sections have consistent scroll animations. Reduced motion is respected. Site matches marko template animation patterns.</done>
</task>

</tasks>

<verification>
1. globals.css has complete CSS variable set matching marko template
2. Hero section animates on page load/scroll
3. All 8 sections have scroll-triggered animations
4. Animation timing is consistent (600ms, easeOut, 0.1s stagger)
5. prefers-reduced-motion is respected
6. No console errors
7. Visual comparison with marko template shows matching animation behavior
</verification>

<success_criteria>
1. Extended font sizes (11xl-17xl) available in CSS
2. Animation timing variables defined
3. Reduced motion media query implemented
4. Hero section uses shared AnimateOnScroll
5. All sections animate consistently
6. Site feels polished with smooth scroll animations
</success_criteria>

<output>
After completion, create `.planning/phases/06-visual-polish/06-03-SUMMARY.md`
</output>
