---
phase: 06-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - irenic-media-new/components/AnimateOnScroll.tsx
  - irenic-media-new/components/Services/Services.tsx
  - irenic-media-new/components/About/About.tsx
  - irenic-media-new/components/WhyUs/WhyUs.tsx
  - irenic-media-new/components/Testimonials/Testimonials.tsx
  - irenic-media-new/components/CaseStudies/CaseStudies.tsx
  - irenic-media-new/components/Pricing/Pricing.tsx
  - irenic-media-new/components/Contact/Contact.tsx
autonomous: true

must_haves:
  truths:
    - "All sections use identical animation timing (600ms duration, easeOut)"
    - "Staggered animations use consistent 0.1s delay increments"
    - "Scroll animations trigger at 15% viewport threshold"
    - "Animation code is not duplicated across components"
  artifacts:
    - path: "irenic-media-new/components/AnimateOnScroll.tsx"
      provides: "Shared animation wrapper component"
      exports: ["AnimateOnScroll", "default"]
  key_links:
    - from: "irenic-media-new/components/Services/Services.tsx"
      to: "irenic-media-new/components/AnimateOnScroll.tsx"
      via: "import AnimateOnScroll"
      pattern: "import.*AnimateOnScroll.*from.*components/AnimateOnScroll"
---

<objective>
Extract and standardize AnimateOnScroll component

Purpose: Eliminate duplicated animation code across 7+ sections and ensure consistent animation timing matching marko template (600ms, easeOut, 0.1s stagger).

Output: Shared AnimateOnScroll component used by all animated sections.
</objective>

<execution_context>
@C:\Users\Raj Shah\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Raj Shah\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-visual-polish/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared AnimateOnScroll component</name>
  <files>irenic-media-new/components/AnimateOnScroll.tsx</files>
  <action>
Create a shared AnimateOnScroll component that consolidates all animation variants currently duplicated across sections.

The component must support these animation types (from Contact.tsx pattern):
- fadeIn: opacity 0->1, y 30->0
- fadeInUp: same as fadeIn (alias)
- fadeInLeft: opacity 0->1, x -50->0
- fadeInRight: opacity 0->1, x 50->0

Props interface:
- children: React.ReactNode
- animation?: 'fadeIn' | 'fadeInUp' | 'fadeInLeft' | 'fadeInRight' (default: 'fadeInUp')
- delay?: number (default: 0)
- duration?: number (default: 0.6) - allows override but defaults to marko timing
- threshold?: number (default: 0.15) - viewport visibility threshold
- className?: string (for wrapper div styling)

Implementation requirements:
- Use 'use client' directive
- Import motion from 'framer-motion' and useInView from 'react-intersection-observer'
- Set triggerOnce: true to prevent re-animation on scroll
- Use ease: 'easeOut' for all transitions (matches marko)
- Export both named and default export

Example usage pattern:
```tsx
<AnimateOnScroll animation="fadeInUp" delay={index * 0.1}>
  <ServiceCard {...service} />
</AnimateOnScroll>
```
  </action>
  <verify>
File exists at irenic-media-new/components/AnimateOnScroll.tsx.
TypeScript compiles: `cd irenic-media-new && npx tsc --noEmit`
  </verify>
  <done>AnimateOnScroll component created with all 4 animation variants, correct timing (600ms, easeOut), and proper TypeScript types.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all sections to use shared AnimateOnScroll</name>
  <files>
    irenic-media-new/components/Services/Services.tsx
    irenic-media-new/components/About/About.tsx
    irenic-media-new/components/WhyUs/WhyUs.tsx
    irenic-media-new/components/Testimonials/Testimonials.tsx
    irenic-media-new/components/CaseStudies/CaseStudies.tsx
    irenic-media-new/components/Pricing/Pricing.tsx
    irenic-media-new/components/Contact/Contact.tsx
  </files>
  <action>
For each file listed, perform these changes:

1. **Remove local AnimateOnScroll function definition** (each file has its own copy)
2. **Add import** at top of file: `import AnimateOnScroll from '@/components/AnimateOnScroll'`
3. **Update usage if needed** - most should work as-is since props are compatible

Specific file notes:
- Services.tsx: Uses fadeInUp (default), no variant prop needed. Keep delay={index * 0.1}
- About.tsx: Uses fadeInLeft and fadeInRight variants. Map 'left'/'right' to 'fadeInLeft'/'fadeInRight'
- WhyUs.tsx: Uses fadeInUp (default), staggered delays
- Testimonials.tsx: Uses fadeInUp (default), staggered delays
- CaseStudies.tsx: Uses fadeInUp (default), staggered delays
- Pricing.tsx: Uses fadeInUp (default), staggered delays
- Contact.tsx: Uses 'fadeIn', 'fadeInLeft', 'fadeInRight' - update variant prop name to animation

After changes, each file should:
- Have NO local AnimateOnScroll function
- Import from '@/components/AnimateOnScroll'
- Use consistent animation prop names
  </action>
  <verify>
Run dev server: `cd irenic-media-new && npm run dev`
Check browser at http://localhost:3000 - scroll through all sections and verify animations work.
No console errors in browser devtools.
  </verify>
  <done>All 7 section components use shared AnimateOnScroll. No duplicate animation code. All sections animate correctly on scroll.</done>
</task>

</tasks>

<verification>
1. Shared AnimateOnScroll component exists at irenic-media-new/components/AnimateOnScroll.tsx
2. No local AnimateOnScroll function definitions in any section component (grep confirms)
3. All sections animate on scroll with consistent timing
4. TypeScript compiles without errors
5. Dev server runs without console errors
</verification>

<success_criteria>
1. Single AnimateOnScroll component used across 7+ sections
2. All animations use 600ms duration, easeOut timing
3. Staggered animations use 0.1s delay increments
4. Zero code duplication for scroll animations
5. Site maintains same visual animation behavior
</success_criteria>

<output>
After completion, create `.planning/phases/06-visual-polish/06-01-SUMMARY.md`
</output>
