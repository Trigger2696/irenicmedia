---
phase: 06-visual-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - irenic-media-new/components/Services/ServiceCard.tsx
  - irenic-media-new/components/Services/Services.tsx
  - irenic-media-new/components/WhyUs/WhyUs.tsx
  - irenic-media-new/components/WhyUs/DifferentiatorCard.tsx
  - irenic-media-new/components/Pricing/Pricing.tsx
  - irenic-media-new/components/Contact/Contact.tsx
autonomous: true

must_haves:
  truths:
    - "All icons use Font Awesome CSS classes"
    - "No lucide-react imports remain in migrated components"
    - "Icon sizes are consistent with original design (text-3xl for cards, text-2xl for details)"
    - "All icons render correctly in both dark and light themes"
  artifacts:
    - path: "irenic-media-new/components/Services/ServiceCard.tsx"
      provides: "Service card with Font Awesome icons"
      contains: "fa-solid"
    - path: "irenic-media-new/components/WhyUs/DifferentiatorCard.tsx"
      provides: "Differentiator card with Font Awesome icons"
      contains: "fa-solid"
  key_links:
    - from: "irenic-media-new/components/Services/Services.tsx"
      to: "ServiceCard"
      via: "icon prop as string (FA class)"
      pattern: "icon:\\s*['\"]fa-"
---

<objective>
Migrate all icons from lucide-react to Font Awesome

Purpose: Match marko template's icon system exactly using Font Awesome 6 CSS classes already available in public/assets/css/vendor/.

Output: All components use Font Awesome icons, lucide-react removed from icon-related components.
</objective>

<execution_context>
@C:\Users\Raj Shah\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Raj Shah\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-visual-polish/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ServiceCard and Services to Font Awesome</name>
  <files>
    irenic-media-new/components/Services/ServiceCard.tsx
    irenic-media-new/components/Services/Services.tsx
  </files>
  <action>
**ServiceCard.tsx changes:**
1. Remove LucideIcon import: `import { LucideIcon } from 'lucide-react'`
2. Change interface from `icon: LucideIcon` to `icon: string` (FA class string)
3. Replace icon rendering:
   - OLD: `<Icon className="w-7 h-7 text-accent" strokeWidth={1.5} />`
   - NEW: `<i className={cn(icon, 'text-3xl text-accent')} />`
4. Keep the icon circle wrapper div with bg-accent/10

**Services.tsx changes:**
1. Remove all lucide-react imports: Share2, Search, TrendingUp, Users, Smartphone, Code
2. Update services array to use Font Awesome class strings:
   ```typescript
   const services = [
     { icon: 'fa-solid fa-share-nodes', title: 'Social Media Marketing', ... },
     { icon: 'fa-solid fa-magnifying-glass', title: 'SEO', ... },
     { icon: 'fa-solid fa-chart-line', title: 'Performance Marketing', ... },
     { icon: 'fa-solid fa-users', title: 'Influencer Campaigns', ... },
     { icon: 'fa-solid fa-mobile-screen', title: 'Mobile App Development', ... },
     { icon: 'fa-solid fa-code', title: 'Custom Software Development', ... }
   ]
   ```

Icon mapping reference (from 06-RESEARCH.md):
- Share2 → fa-share-nodes
- Search → fa-magnifying-glass
- TrendingUp → fa-chart-line
- Users → fa-users
- Smartphone → fa-mobile-screen
- Code → fa-code
  </action>
  <verify>
Run dev server, navigate to Services section.
All 6 service cards display icons correctly.
Icons match the visual style (purple/accent color, rounded bg).
  </verify>
  <done>Services section uses Font Awesome icons. No lucide-react imports. All 6 icons display correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate WhyUs/DifferentiatorCard to Font Awesome</name>
  <files>
    irenic-media-new/components/WhyUs/DifferentiatorCard.tsx
    irenic-media-new/components/WhyUs/WhyUs.tsx
  </files>
  <action>
**DifferentiatorCard.tsx changes:**
1. Remove LucideIcon import
2. Change interface from `icon: LucideIcon` to `icon: string`
3. Replace icon rendering:
   - OLD: `<Icon className="w-7 h-7 text-accent" strokeWidth={1.5} />`
   - NEW: `<i className={cn(icon, 'text-3xl text-accent')} />`
4. Import cn from '@/lib/utils' if not already imported

**WhyUs.tsx changes:**
1. Remove all lucide-react imports: Lightbulb, BarChart3, Handshake, Zap
2. Update differentiators array to use Font Awesome class strings:
   ```typescript
   const differentiators = [
     { icon: 'fa-solid fa-lightbulb', title: 'Strategy Before Execution', ... },
     { icon: 'fa-solid fa-chart-simple', title: 'Data-Driven Decisions', ... },
     { icon: 'fa-solid fa-handshake', title: 'Transparent Partnership', ... },
     { icon: 'fa-solid fa-bolt', title: 'Agile & Adaptive', ... }
   ]
   ```

Icon mapping reference:
- Lightbulb → fa-lightbulb
- BarChart3 → fa-chart-simple
- Handshake → fa-handshake
- Zap → fa-bolt
  </action>
  <verify>
Navigate to Why Choose Us section.
All 4 differentiator cards display icons correctly.
Stat badges still display correctly alongside icons.
  </verify>
  <done>WhyUs section uses Font Awesome icons. All 4 differentiator icons display correctly with stats.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate Pricing and Contact to Font Awesome</name>
  <files>
    irenic-media-new/components/Pricing/Pricing.tsx
    irenic-media-new/components/Contact/Contact.tsx
  </files>
  <action>
**Pricing.tsx changes:**
1. Remove lucide-react import: `import { Check } from 'lucide-react'`
2. Replace Check icon in feature list rendering:
   - OLD: `<Check className="w-5 h-5 text-accent flex-shrink-0" />`
   - NEW: `<i className="fa-solid fa-check text-lg text-accent flex-shrink-0" />`

Note: text-lg (~18px) matches the w-5 h-5 (20px) reasonably well.

**Contact.tsx changes:**
1. Remove lucide-react imports: `import { Mail, Phone, MapPin } from 'lucide-react'`
2. Update contactDetails array to use Font Awesome strings:
   ```typescript
   const contactDetails = [
     { icon: 'fa-solid fa-envelope', label: 'Email', value: 'hello@irenicmedia.com' },
     { icon: 'fa-solid fa-phone', label: 'Phone', value: '+91 98765 43210' },
     { icon: 'fa-solid fa-location-dot', label: 'Location', value: 'Mumbai, India' }
   ]
   ```
3. Update the map rendering at lines 92-96 (inside contactDetails.map callback):
   - Line 93: Remove `const Icon = detail.icon`
   - Line 96-97 area: Find `<Icon className="w-6 h-6 text-accent" />` inside the icon circle div
   - Replace Icon component with: `<i className={cn(detail.icon, 'text-2xl text-accent')} />`
4. Import cn from '@/lib/utils' if not already imported

Icon mapping reference:
- Check → fa-check
- Mail → fa-envelope
- Phone → fa-phone
- MapPin → fa-location-dot
  </action>
  <verify>
Navigate to Pricing section - check marks appear next to all features.
Navigate to Contact section - mail, phone, location icons display correctly.
Icons are properly sized and colored.
  </verify>
  <done>Pricing and Contact sections use Font Awesome icons. All icons render correctly.</done>
</task>

</tasks>

<verification>
1. No lucide-react imports in: ServiceCard, Services, DifferentiatorCard, WhyUs, Pricing, Contact
2. All icons render as Font Awesome (check browser devtools - should see `<i class="fa-solid fa-...">`)
3. Icon sizes are consistent with previous design
4. Icons are accent-colored in dark mode
5. Icons remain visible in light mode
</verification>

<success_criteria>
1. Zero lucide-react imports in migrated components
2. All icons use Font Awesome fa-solid or fa-brands classes
3. Visual appearance matches previous design (size, color, positioning)
4. Icons render correctly in both dark and light themes
5. No console errors related to icons
</success_criteria>

<output>
After completion, create `.planning/phases/06-visual-polish/06-02-SUMMARY.md`
</output>
