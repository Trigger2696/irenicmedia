---
phase: 04-social-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - irenic-media-new/components/Testimonials/TestimonialCard.tsx
  - irenic-media-new/components/Testimonials/Testimonials.tsx
  - irenic-media-new/public/images/testimonials/.gitkeep
autonomous: true

must_haves:
  truths:
    - "User can scroll to testimonials section and see 3-4 client reviews"
    - "User sees client photo, name, company, role for each testimonial"
    - "User sees star rating displayed for each testimonial"
    - "Testimonial cards animate on scroll (fade-in with stagger)"
  artifacts:
    - path: "irenic-media-new/components/Testimonials/TestimonialCard.tsx"
      provides: "Individual testimonial card with photo, name, role, company, star rating, review text"
      exports: ["TestimonialCard"]
    - path: "irenic-media-new/components/Testimonials/Testimonials.tsx"
      provides: "Testimonials section with responsive grid and scroll animations"
      exports: ["default"]
  key_links:
    - from: "irenic-media-new/components/Testimonials/Testimonials.tsx"
      to: "TestimonialCard"
      via: "import and render in grid"
      pattern: "import.*TestimonialCard"
    - from: "irenic-media-new/components/Testimonials/TestimonialCard.tsx"
      to: "next/image"
      via: "Image component for client photos"
      pattern: "import Image from 'next/image'"
---

<objective>
Create Testimonials section with client testimonial cards showing photos, ratings, and reviews.

Purpose: Display social proof from clients to build credibility and trust.
Output: TestimonialCard component and Testimonials section with 3-4 fictional client testimonials.
</objective>

<execution_context>
@C:\Users\Raj Shah\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Raj Shah\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-social-proof/04-RESEARCH.md

# Existing patterns to follow:
@irenic-media-new/components/Services/Services.tsx - AnimateOnScroll pattern, grid layout, section structure
@irenic-media-new/components/Services/ServiceCard.tsx - Card styling with Tailwind CSS vars
@irenic-media-new/components/WhyUs/DifferentiatorCard.tsx - Card with icon and metadata pattern
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TestimonialCard component</name>
  <files>irenic-media-new/components/Testimonials/TestimonialCard.tsx</files>
  <action>
Create TestimonialCard component with:
- Props interface: name, company, role, rating (1-5), review, image (string path)
- Use `'use client'` directive at top
- Import `Image` from `next/image` for client photo
- Import `Star` from `lucide-react` for rating display
- Import `cn` from `@/lib/utils` for class merging

Structure:
1. Card wrapper with same styling pattern as ServiceCard:
   - `p-6 rounded-marko bg-[var(--body-bg)] border border-[var(--accent-color-3)]`
   - Hover effect: `hover:shadow-accent hover:-translate-y-1 transition-all duration-300`

2. Star rating at top:
   - Container with `role="img"` and `aria-label="${rating} out of 5 stars"` for accessibility
   - Render 5 Star icons in flex row with gap-1
   - Filled stars (index < rating): `fill-[var(--accent-color)] text-accent`
   - Empty stars: `text-[var(--accent-color-3)]`
   - Star size: 18px

3. Client info section (flex row):
   - Circular photo using next/image with `fill` prop:
     - Container: `relative w-14 h-14 flex-shrink-0`
     - Image: `rounded-full object-cover`
     - sizes: "56px"
     - alt: "{name} profile photo"
   - Text info (flex-1):
     - Name: `text-lg font-bold text-primary`
     - Role: `text-sm text-[var(--text-color)]`
     - Company: `text-sm text-accent`

4. Review text:
   - blockquote element
   - `text-[var(--text-color)] leading-relaxed mt-4`
   - Wrap text in quotes: "{review}"

Export as named export: `export function TestimonialCard`
  </action>
  <verify>
File exists at irenic-media-new/components/Testimonials/TestimonialCard.tsx
TypeScript compiles without errors
Star rating has proper aria-label for accessibility
  </verify>
  <done>
TestimonialCard component renders client photo, name, role, company, star rating, and review text with proper styling matching existing card patterns.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Testimonials section</name>
  <files>
irenic-media-new/components/Testimonials/Testimonials.tsx
irenic-media-new/public/images/testimonials/.gitkeep
  </files>
  <action>
Create folder structure first:
- mkdir -p irenic-media-new/public/images/testimonials
- Create .gitkeep file in that folder

Create Testimonials.tsx with:
- `'use client'` directive
- Import: motion from framer-motion
- Import: useInView from react-intersection-observer
- Import: TestimonialCard from ./TestimonialCard

Define testimonials data array (3-4 fictional clients relevant to digital agency):
```typescript
const testimonials = [
  {
    name: 'Sarah Chen',
    company: 'TechFlow Solutions',
    role: 'Marketing Director',
    rating: 5,
    review: 'Irenic Media transformed our digital presence. Their strategic approach resulted in a 150% increase in qualified leads within just 6 months. The team truly understands how to blend creativity with data-driven decisions.',
    image: '/images/testimonials/placeholder-1.jpg'
  },
  {
    name: 'Michael Rodriguez',
    company: 'GreenLeaf Ventures',
    role: 'Founder & CEO',
    rating: 5,
    review: 'Working with Irenic Media has been a game-changer. They don\'t just execute campaigns – they become true partners in your growth journey. Our ROI has tripled since we started working together.',
    image: '/images/testimonials/placeholder-2.jpg'
  },
  {
    name: 'Priya Sharma',
    company: 'Bloom Fashion',
    role: 'Brand Manager',
    rating: 5,
    review: 'The transparency and communication from Irenic Media is unmatched. We always know exactly what\'s happening with our campaigns and why. They\'ve helped us build a loyal community of over 100k followers.',
    image: '/images/testimonials/placeholder-3.jpg'
  },
  {
    name: 'David Kim',
    company: 'FitnessPro App',
    role: 'Head of Growth',
    rating: 4,
    review: 'From app development to user acquisition, Irenic Media handled everything seamlessly. Our app downloads increased by 200% in the first quarter after launch. Highly recommend their mobile expertise.',
    image: '/images/testimonials/placeholder-4.jpg'
  }
]
```

Create AnimateOnScroll wrapper (copy pattern from Services.tsx):
```typescript
function AnimateOnScroll({
  children,
  delay = 0
}: {
  children: React.ReactNode
  delay?: number
}) {
  const { ref, inView } = useInView({
    triggerOnce: true,
    threshold: 0.15
  })

  return (
    <motion.div
      ref={ref}
      initial={{ opacity: 0, y: 30 }}
      animate={inView ? { opacity: 1, y: 0 } : { opacity: 0, y: 30 }}
      transition={{ duration: 0.6, delay }}
    >
      {children}
    </motion.div>
  )
}
```

Section structure (follow Services.tsx pattern):
- section with id="testimonials", scrollMarginTop: 100px
- bg-[var(--body-bg)] for alternating section background
- hero-container div
- Section header with star icon, "Client Stories" label, heading "What Our Clients Say", description paragraph
- Responsive grid: grid-cols-1 md:grid-cols-2 gap-6

Render testimonials in grid (EXPLICIT WIRING):
```tsx
<div className="grid grid-cols-1 md:grid-cols-2 gap-6">
  {testimonials.map((testimonial, index) => (
    <AnimateOnScroll key={testimonial.name} delay={index * 0.1}>
      <TestimonialCard {...testimonial} />
    </AnimateOnScroll>
  ))}
</div>
```

Export as default export.
  </action>
  <verify>
File exists at irenic-media-new/components/Testimonials/Testimonials.tsx
Folder exists at irenic-media-new/public/images/testimonials/
TypeScript compiles without errors: cd irenic-media-new && npx tsc --noEmit
  </verify>
  <done>
Testimonials section displays 4 client testimonials in a 2-column responsive grid with scroll animations and staggered delays.
  </done>
</task>

</tasks>

<verification>
1. TestimonialCard component exists with proper props interface
2. Star rating has accessibility attributes (role="img", aria-label)
3. next/image used for client photos with proper sizes prop
4. Testimonials section follows established section pattern (header, description, grid)
5. AnimateOnScroll pattern reused from existing sections
6. Responsive grid: 1 col mobile, 2 cols md and up
7. TypeScript compiles without errors
</verification>

<success_criteria>
- TestimonialCard renders photo, name, role, company, star rating, review
- Testimonials section displays 4 testimonials in responsive grid
- Cards animate on scroll with staggered delays (0.1s increments)
- Star ratings display correctly (filled vs empty stars)
- All accessibility requirements met (aria-label on ratings)
- Styling consistent with existing ServiceCard and DifferentiatorCard
</success_criteria>

<output>
After completion, create `.planning/phases/04-social-proof/04-01-SUMMARY.md`
</output>
