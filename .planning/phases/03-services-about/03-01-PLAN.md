---
phase: 03-services-about
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - irenic-media-new/components/Services/ServiceCard.tsx
  - irenic-media-new/components/Services/Services.tsx
  - irenic-media-new/app/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can scroll to services section and see all 6 services in a responsive grid"
    - "User can read service descriptions with clear icons for each service"
    - "Service cards fade in as user scrolls to them"
    - "Grid displays 1 column on mobile, 2 on tablet, 3 on desktop"
  artifacts:
    - path: "irenic-media-new/components/Services/ServiceCard.tsx"
      provides: "Reusable service card component with icon, title, description"
      exports: ["ServiceCard"]
    - path: "irenic-media-new/components/Services/Services.tsx"
      provides: "Services section with responsive grid and animations"
      exports: ["default"]
    - path: "irenic-media-new/app/page.tsx"
      provides: "Page with Services component integrated"
      contains: "import Services"
  key_links:
    - from: "Services.tsx"
      to: "ServiceCard.tsx"
      via: "component import"
      pattern: "import.*ServiceCard"
    - from: "Services.tsx"
      to: "lucide-react"
      via: "icon imports"
      pattern: "import.*from 'lucide-react'"
    - from: "Services.tsx"
      to: "framer-motion"
      via: "animation import"
      pattern: "import.*motion.*from 'framer-motion'"
---

<objective>
Create the Services section with 6 service cards in a responsive grid layout with scroll-triggered animations.

Purpose: Display Irenic Media's service offerings clearly with icons, titles, and descriptions so visitors understand what the agency provides.
Output: Working Services section visible on the home page with all 4 SERV requirements met.
</objective>

<execution_context>
@C:\Users\Raj Shah\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Raj Shah\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-services-about/03-RESEARCH.md
@irenic-media-new/components/Hero/Hero.tsx
@irenic-media-new/app/page.tsx
@irenic-media-new/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ServiceCard and Services components</name>
  <files>irenic-media-new/components/Services/ServiceCard.tsx, irenic-media-new/components/Services/Services.tsx</files>
  <action>
Create the Services folder and components.

**Part A: Create ServiceCard.tsx**

Create file `components/Services/ServiceCard.tsx`:

```typescript
'use client'

import { LucideIcon } from 'lucide-react'
import { cn } from '@/lib/utils'

interface ServiceCardProps {
  icon: LucideIcon
  title: string
  description: string
  className?: string
}

export function ServiceCard({ icon: Icon, title, description, className }: ServiceCardProps) {
  return (
    <div className={cn(
      'p-6 rounded-marko bg-[var(--body-bg)] border border-[var(--accent-color-3)]',
      'transition-all duration-300 hover:shadow-accent hover:-translate-y-1',
      className
    )}>
      {/* Icon */}
      <div className="w-14 h-14 rounded-full bg-[var(--accent-color)] bg-opacity-10 flex items-center justify-center mb-4">
        <Icon className="w-7 h-7 text-accent" />
      </div>

      {/* Content */}
      <h3 className="text-xl font-bold text-primary mb-3">
        {title}
      </h3>
      <p className="text-[var(--text-color)] leading-relaxed">
        {description}
      </p>
    </div>
  )
}
```

**Part B: Create Services.tsx**

Create file `components/Services/Services.tsx`:

```typescript
'use client'

import { motion } from 'framer-motion'
import { useInView } from 'react-intersection-observer'
import { ServiceCard } from './ServiceCard'
import {
  Share2,      // Social Media
  Search,      // SEO
  TrendingUp,  // Performance Marketing
  Users,       // Influencer
  Smartphone,  // Mobile App
  Code         // Custom Software
} from 'lucide-react'

const services = [
  {
    icon: Share2,
    title: 'Social Media Marketing',
    description: 'Strategic social media management that builds engaged communities and drives meaningful conversions across all platforms.'
  },
  {
    icon: Search,
    title: 'SEO',
    description: 'Data-driven SEO strategies that improve your search rankings and deliver sustainable organic growth over time.'
  },
  {
    icon: TrendingUp,
    title: 'Performance Marketing',
    description: 'ROI-focused campaigns across paid channels, optimized continuously for maximum performance and efficiency.'
  },
  {
    icon: Users,
    title: 'Influencer Campaigns',
    description: 'Authentic influencer partnerships that amplify your brand message and reach your target audiences effectively.'
  },
  {
    icon: Smartphone,
    title: 'Mobile App Development',
    description: 'End-to-end mobile app solutions from concept to launch, with ongoing marketing and user acquisition support.'
  },
  {
    icon: Code,
    title: 'Custom Software Development',
    description: 'Bespoke software solutions designed and built to solve your unique business challenges and drive efficiency.'
  }
]

// AnimateOnScroll wrapper for scroll-triggered animations
function AnimateOnScroll({
  children,
  delay = 0
}: {
  children: React.ReactNode
  delay?: number
}) {
  const { ref, inView } = useInView({
    triggerOnce: true,
    threshold: 0.15
  })

  return (
    <motion.div
      ref={ref}
      initial={{ opacity: 0, y: 30 }}
      animate={inView ? { opacity: 1, y: 0 } : { opacity: 0, y: 30 }}
      transition={{ duration: 0.6, delay }}
    >
      {children}
    </motion.div>
  )
}

export default function Services() {
  return (
    <section
      id="services"
      className="section bg-[var(--accent-color-3)]"
      style={{ scrollMarginTop: '100px' }}
    >
      <div className="hero-container">
        {/* Section Header */}
        <div className="flex items-center gap-3 mb-4">
          <i className="fa-solid fa-star text-accent"></i>
          <span className="text-primary font-bold">Our Services</span>
        </div>
        <h2 className="text-3xl md:text-4xl font-bold text-primary mb-4">
          What We <span className="accent-color">Offer</span>
        </h2>
        <p className="text-[var(--text-color)] mb-12 max-w-2xl">
          Comprehensive digital marketing and technology solutions tailored to your business needs.
        </p>

        {/* Services Grid - Mobile-first responsive: 1 col -> 2 cols -> 3 cols */}
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          {services.map((service, index) => (
            <AnimateOnScroll
              key={service.title}
              delay={index * 0.1}
            >
              <ServiceCard {...service} />
            </AnimateOnScroll>
          ))}
        </div>
      </div>
    </section>
  )
}
```

Note: The cn utility should already exist from prior phases. If not, create `lib/utils.ts`:
```typescript
import { clsx, type ClassValue } from 'clsx'
import { twMerge } from 'tailwind-merge'

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```
  </action>
  <verify>
1. Check folder and files created: `ls irenic-media-new/components/Services/`
2. TypeScript compiles: `cd irenic-media-new && npx tsc --noEmit`
  </verify>
  <done>ServiceCard and Services components created with responsive grid and fade-in animations</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Services into page.tsx</name>
  <files>irenic-media-new/app/page.tsx</files>
  <action>
Replace the services placeholder section in page.tsx with the Services component.

1. Add import at top (after existing imports):
```typescript
import Services from '@/components/Services/Services'
```

2. Find the existing services section placeholder (lines ~17-30 with `id="services"`):
```tsx
{/* Services Section */}
<section id="services" className="section bg-[var(--accent-color-3)]" style={{ scrollMarginTop: '100px' }}>
  <div className="hero-container">
    ...placeholder content...
  </div>
</section>
```

3. Replace the entire services section with:
```tsx
{/* Services Section */}
<Services />
```

Keep all other sections (about, why-us, testimonials, etc.) unchanged.
  </action>
  <verify>
1. `npm run build` succeeds in irenic-media-new directory
2. `npm run dev` starts without errors
3. Visit http://localhost:3000 and verify:
   - Scroll to services section shows 6 cards
   - Each card has icon, title, and description
   - Grid is 1 column on mobile, 2 on tablet, 3 on desktop
   - Cards fade in as you scroll to them
   - Services listed: Social Media, SEO, Performance Marketing, Influencer, Mobile App, Custom Software
  </verify>
  <done>Services section displays 6 service cards in responsive grid with scroll animations</done>
</task>

</tasks>

<verification>
Manual verification checklist:
1. [ ] Services section visible when scrolling down from hero
2. [ ] 6 service cards displayed in grid layout
3. [ ] Each card shows: icon, title (bold), description
4. [ ] Grid responsive: 1 col mobile, 2 col tablet (md), 3 col desktop (lg)
5. [ ] Cards animate (fade in from below) when scrolled into view
6. [ ] Animation only plays once (triggerOnce: true)
7. [ ] All 6 services present: Social Media Marketing, SEO, Performance Marketing, Influencer Campaigns, Mobile App Development, Custom Software Development
8. [ ] Cards have hover effect (slight lift + shadow)
9. [ ] No console errors
</verification>

<success_criteria>
All 4 SERV requirements satisfied:
- SERV-01: 6 service cards in responsive grid - VERIFIED
- SERV-02: Each service card shows icon, title, description - VERIFIED
- SERV-03: All 6 services displayed - VERIFIED
- SERV-04: Service cards animate on scroll (fade-in) - VERIFIED
</success_criteria>

<output>
After completion, create `.planning/phases/03-services-about/03-01-SUMMARY.md`
</output>
