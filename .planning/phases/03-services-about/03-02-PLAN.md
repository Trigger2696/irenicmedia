---
phase: 03-services-about
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - irenic-media-new/components/About/About.tsx
  - irenic-media-new/components/WhyUs/DifferentiatorCard.tsx
  - irenic-media-new/components/WhyUs/WhyUs.tsx
  - irenic-media-new/app/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can scroll to about section and learn about founders and company values"
    - "User sees two-column layout on desktop that stacks on mobile"
    - "User can view 4 differentiator cards explaining why to choose Irenic Media"
    - "Each differentiator card shows icon, title, description, and stat"
  artifacts:
    - path: "irenic-media-new/components/About/About.tsx"
      provides: "About section with founder story and company values in two-column layout"
      exports: ["default"]
    - path: "irenic-media-new/components/WhyUs/DifferentiatorCard.tsx"
      provides: "Card component with icon, title, description, and stat badge"
      exports: ["DifferentiatorCard"]
    - path: "irenic-media-new/components/WhyUs/WhyUs.tsx"
      provides: "Why Us section with 4 differentiator cards in responsive grid"
      exports: ["default"]
    - path: "irenic-media-new/app/page.tsx"
      provides: "Page with About and WhyUs components integrated"
      contains: ["import About", "import WhyUs"]
  key_links:
    - from: "About.tsx"
      to: "framer-motion"
      via: "animation import"
      pattern: "import.*motion.*from 'framer-motion'"
    - from: "WhyUs.tsx"
      to: "DifferentiatorCard.tsx"
      via: "component import"
      pattern: "import.*DifferentiatorCard"
    - from: "WhyUs.tsx"
      to: "lucide-react"
      via: "icon imports"
      pattern: "import.*from 'lucide-react'"
    - from: "page.tsx"
      to: "About.tsx"
      via: "component import"
      pattern: "import About"
    - from: "page.tsx"
      to: "WhyUs.tsx"
      via: "component import"
      pattern: "import WhyUs"
---

<objective>
Create the About section with founder story and company values, plus the Why Us section with 4 differentiator cards.

Purpose: Communicate who Irenic Media is (founders, values) and why clients should choose them (differentiators with proof points).
Output: Working About and Why Us sections visible on the home page with all 6 requirements met (ABOUT-01 to ABOUT-03, WHY-01 to WHY-03).
</objective>

<execution_context>
@C:\Users\Raj Shah\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Raj Shah\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-services-about/03-RESEARCH.md
@.planning/phases/03-services-about/03-01-SUMMARY.md
@irenic-media-new/components/Services/Services.tsx
@irenic-media-new/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create About section component</name>
  <files>irenic-media-new/components/About/About.tsx</files>
  <action>
Create the About folder and component with two-column layout.

Create file `components/About/About.tsx`:

```typescript
'use client'

import { motion } from 'framer-motion'
import { useInView } from 'react-intersection-observer'

// AnimateOnScroll wrapper for scroll-triggered animations
function AnimateOnScroll({
  children,
  animation = 'fadeInUp',
  delay = 0
}: {
  children: React.ReactNode
  animation?: 'fadeInUp' | 'fadeInLeft' | 'fadeInRight'
  delay?: number
}) {
  const { ref, inView } = useInView({
    triggerOnce: true,
    threshold: 0.15
  })

  const variants = {
    fadeInUp: {
      hidden: { opacity: 0, y: 30 },
      visible: { opacity: 1, y: 0 }
    },
    fadeInLeft: {
      hidden: { opacity: 0, x: -50 },
      visible: { opacity: 1, x: 0 }
    },
    fadeInRight: {
      hidden: { opacity: 0, x: 50 },
      visible: { opacity: 1, x: 0 }
    }
  }

  return (
    <motion.div
      ref={ref}
      initial="hidden"
      animate={inView ? 'visible' : 'hidden'}
      variants={variants[animation]}
      transition={{ duration: 0.6, delay }}
    >
      {children}
    </motion.div>
  )
}

export default function About() {
  return (
    <section
      id="about"
      className="section"
      style={{ scrollMarginTop: '100px' }}
    >
      <div className="hero-container">
        {/* Section Header */}
        <div className="flex items-center gap-3 mb-4">
          <i className="fa-solid fa-star text-accent"></i>
          <span className="text-primary font-bold">About Us</span>
        </div>
        <h2 className="text-3xl md:text-4xl font-bold text-primary mb-12">
          Who We <span className="accent-color">Are</span>
        </h2>

        {/* Two-column layout: stacks on mobile, side-by-side on desktop */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12">
          {/* Left Column: Founder Story */}
          <AnimateOnScroll animation="fadeInLeft">
            <div>
              <h3 className="text-2xl font-bold text-primary mb-4">
                Our Story
              </h3>
              <p className="text-[var(--text-color)] mb-4 leading-relaxed">
                Founded by <span className="text-accent font-semibold">Raj Shah</span> and{' '}
                <span className="text-accent font-semibold">Ruchika Chandel</span>, Irenic Media
                was born from a simple belief: that great marketing starts with great strategy.
              </p>
              <p className="text-[var(--text-color)] mb-4 leading-relaxed">
                We saw too many brands chasing tactics without purpose, running campaigns without
                clarity. We built Irenic Media to be different—a place where strategy comes first,
                creativity has intent, and growth is built to last.
              </p>
              <p className="text-[var(--text-color)] leading-relaxed">
                Today, we partner with ambitious brands across industries, helping them cut through
                the noise and achieve sustainable, long-term growth.
              </p>
            </div>
          </AnimateOnScroll>

          {/* Right Column: Company Values */}
          <AnimateOnScroll animation="fadeInRight" delay={0.2}>
            <div>
              <h3 className="text-2xl font-bold text-primary mb-6">
                Our Values
              </h3>
              <div className="space-y-6">
                <div className="p-4 rounded-marko bg-[var(--accent-color-3)] border border-[var(--accent-color-3)]">
                  <h4 className="text-lg font-bold text-accent mb-2">
                    Strategy First
                  </h4>
                  <p className="text-[var(--text-color)] leading-relaxed">
                    We don't jump into tactics. Every campaign begins with deep strategic
                    thinking—understanding your goals, audience, and competitive landscape.
                  </p>
                </div>
                <div className="p-4 rounded-marko bg-[var(--accent-color-3)] border border-[var(--accent-color-3)]">
                  <h4 className="text-lg font-bold text-accent mb-2">
                    Creative Intent
                  </h4>
                  <p className="text-[var(--text-color)] leading-relaxed">
                    Our creativity serves a purpose. Every design decision, every piece of
                    content is backed by data and aligned with your objectives.
                  </p>
                </div>
                <div className="p-4 rounded-marko bg-[var(--accent-color-3)] border border-[var(--accent-color-3)]">
                  <h4 className="text-lg font-bold text-accent mb-2">
                    Long-term Growth
                  </h4>
                  <p className="text-[var(--text-color)] leading-relaxed">
                    We build for the long haul. Sustainable growth beats short-term wins
                    every time—we're here to help you scale responsibly.
                  </p>
                </div>
              </div>
            </div>
          </AnimateOnScroll>
        </div>
      </div>
    </section>
  )
}
```
  </action>
  <verify>
1. Check folder and file created: `ls irenic-media-new/components/About/`
2. TypeScript compiles: `cd irenic-media-new && npx tsc --noEmit`
  </verify>
  <done>About section component created with two-column layout containing founder story and 3 company values</done>
</task>

<task type="auto">
  <name>Task 2: Create DifferentiatorCard and WhyUs components</name>
  <files>irenic-media-new/components/WhyUs/DifferentiatorCard.tsx, irenic-media-new/components/WhyUs/WhyUs.tsx</files>
  <action>
Create the WhyUs folder and components.

**Part A: Create DifferentiatorCard.tsx**

Create file `components/WhyUs/DifferentiatorCard.tsx`:

```typescript
'use client'

import { LucideIcon } from 'lucide-react'

interface DifferentiatorCardProps {
  icon: LucideIcon
  title: string
  description: string
  stat: string
  statLabel: string
}

export function DifferentiatorCard({
  icon: Icon,
  title,
  description,
  stat,
  statLabel
}: DifferentiatorCardProps) {
  return (
    <div className="p-6 rounded-marko bg-[var(--body-bg)] border border-[var(--accent-color-3)] transition-all duration-300 hover:shadow-accent hover:-translate-y-1">
      {/* Icon with stat badge */}
      <div className="flex items-start justify-between mb-4">
        <div className="w-14 h-14 rounded-full bg-[var(--accent-color)] bg-opacity-10 flex items-center justify-center">
          <Icon className="w-7 h-7 text-accent" />
        </div>
        <div className="text-right">
          <div className="text-2xl font-bold text-accent">{stat}</div>
          <div className="text-xs text-[var(--text-color)]">{statLabel}</div>
        </div>
      </div>

      {/* Content */}
      <h3 className="text-xl font-bold text-primary mb-3">
        {title}
      </h3>
      <p className="text-[var(--text-color)] leading-relaxed">
        {description}
      </p>
    </div>
  )
}
```

**Part B: Create WhyUs.tsx**

Create file `components/WhyUs/WhyUs.tsx`:

```typescript
'use client'

import { motion } from 'framer-motion'
import { useInView } from 'react-intersection-observer'
import { DifferentiatorCard } from './DifferentiatorCard'
import { Lightbulb, BarChart3, Handshake, Zap } from 'lucide-react'

const differentiators = [
  {
    icon: Lightbulb,
    title: 'Strategy Before Execution',
    description: 'We don\'t jump into tactics. Every campaign starts with deep strategic thinking, clear objectives, and a roadmap for success.',
    stat: '100%',
    statLabel: 'Strategy First'
  },
  {
    icon: BarChart3,
    title: 'Data-Driven Decisions',
    description: 'Our decisions are backed by data, not guesswork. We measure everything that matters and optimize based on real insights.',
    stat: '24/7',
    statLabel: 'Monitoring'
  },
  {
    icon: Handshake,
    title: 'Transparent Partnership',
    description: 'No black boxes. We share our thinking, our data, and our learnings openly. You\'ll always know what we\'re doing and why.',
    stat: '95%',
    statLabel: 'Client Retention'
  },
  {
    icon: Zap,
    title: 'Agile & Adaptive',
    description: 'Markets change fast. We stay nimble, continuously testing and iterating to keep you ahead of the curve.',
    stat: '2x',
    statLabel: 'Faster Iteration'
  }
]

// AnimateOnScroll wrapper for scroll-triggered animations
function AnimateOnScroll({
  children,
  delay = 0
}: {
  children: React.ReactNode
  delay?: number
}) {
  const { ref, inView } = useInView({
    triggerOnce: true,
    threshold: 0.15
  })

  return (
    <motion.div
      ref={ref}
      initial={{ opacity: 0, y: 30 }}
      animate={inView ? { opacity: 1, y: 0 } : { opacity: 0, y: 30 }}
      transition={{ duration: 0.6, delay }}
    >
      {children}
    </motion.div>
  )
}

export default function WhyUs() {
  return (
    <section
      id="why-us"
      className="section bg-[var(--accent-color-3)]"
      style={{ scrollMarginTop: '100px' }}
    >
      <div className="hero-container">
        {/* Section Header */}
        <div className="flex items-center gap-3 mb-4">
          <i className="fa-solid fa-star text-accent"></i>
          <span className="text-primary font-bold">Why Choose Us</span>
        </div>
        <h2 className="text-3xl md:text-4xl font-bold text-primary mb-4">
          Why <span className="accent-color">Irenic Media</span>
        </h2>
        <p className="text-[var(--text-color)] mb-12 max-w-2xl">
          We're not just another agency. Here's what makes us different.
        </p>

        {/* Differentiators Grid - Mobile-first responsive: 1 col -> 2 cols -> 4 cols */}
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
          {differentiators.map((diff, index) => (
            <AnimateOnScroll key={diff.title} delay={index * 0.1}>
              <DifferentiatorCard {...diff} />
            </AnimateOnScroll>
          ))}
        </div>
      </div>
    </section>
  )
}
```
  </action>
  <verify>
1. Check folder and files created: `ls irenic-media-new/components/WhyUs/`
2. TypeScript compiles: `cd irenic-media-new && npx tsc --noEmit`
  </verify>
  <done>DifferentiatorCard and WhyUs components created with 4 differentiator cards showing icons, titles, descriptions, and stats</done>
</task>

<task type="auto">
  <name>Task 3: Integrate About and WhyUs into page.tsx</name>
  <files>irenic-media-new/app/page.tsx</files>
  <action>
Replace the about and why-us placeholder sections in page.tsx with the new components.

1. Add imports at top (after Services import):
```typescript
import About from '@/components/About/About'
import WhyUs from '@/components/WhyUs/WhyUs'
```

2. Find the existing about section placeholder (with `id="about"`):
```tsx
{/* About Section */}
<section id="about" className="section" style={{ scrollMarginTop: '100px' }}>
  <div className="hero-container">
    ...placeholder content...
  </div>
</section>
```

3. Replace the entire about section with:
```tsx
{/* About Section */}
<About />
```

4. Find the existing why-us section placeholder (with `id="why-us"`):
```tsx
{/* Why Us Section */}
<section id="why-us" className="section bg-[var(--accent-color-3)]" style={{ scrollMarginTop: '100px' }}>
  <div className="hero-container">
    ...placeholder content...
  </div>
</section>
```

5. Replace the entire why-us section with:
```tsx
{/* Why Us Section */}
<WhyUs />
```

Keep all other sections (testimonials, case-studies, pricing, contact) unchanged.

Final order in page.tsx should be:
- Header
- Spacer
- Hero
- Services
- About
- WhyUs
- Testimonials (placeholder)
- Case Studies (placeholder)
- Pricing (placeholder)
- Contact (placeholder)
- Footer
  </action>
  <verify>
1. `npm run build` succeeds in irenic-media-new directory
2. `npm run dev` starts without errors
3. Visit http://localhost:3000 and verify:
   - About section shows founder names (Raj Shah, Ruchika Chandel)
   - About section shows 3 values (Strategy First, Creative Intent, Long-term Growth)
   - About section has two-column layout on desktop, stacks on mobile
   - Why Us section shows 4 differentiator cards
   - Each differentiator card has icon, title, description, and stat
   - All 4 differentiators present: Strategy Before Execution, Data-Driven Decisions, Transparent Partnership, Agile & Adaptive
  </verify>
  <done>About and WhyUs sections integrated into page, replacing placeholders</done>
</task>

</tasks>

<verification>
Manual verification checklist:

About Section:
1. [ ] About section visible when scrolling past services
2. [ ] Founder names displayed: Raj Shah and Ruchika Chandel (highlighted in accent color)
3. [ ] Story text explains founding vision
4. [ ] Three company values displayed: Strategy First, Creative Intent, Long-term Growth
5. [ ] Two-column layout on desktop (story left, values right)
6. [ ] Stacks to single column on mobile
7. [ ] Values in card-style boxes with accent headers
8. [ ] Animations trigger on scroll

Why Us Section:
9. [ ] Why Us section visible after About section
10. [ ] 4 differentiator cards displayed in grid
11. [ ] Grid responsive: 1 col mobile, 2 tablet, 4 desktop
12. [ ] Each card shows icon, title, description, AND stat badge
13. [ ] All 4 differentiators present with correct stats:
    - Strategy Before Execution (100% Strategy First)
    - Data-Driven Decisions (24/7 Monitoring)
    - Transparent Partnership (95% Client Retention)
    - Agile & Adaptive (2x Faster Iteration)
14. [ ] Cards animate on scroll with staggered delays
15. [ ] Cards have hover effect (lift + shadow)

General:
16. [ ] No console errors
17. [ ] Smooth scroll from nav links to sections still works
</verification>

<success_criteria>
All 6 requirements satisfied:
- ABOUT-01: Founder story (Raj Shah & Ruchika Chandel) - VERIFIED
- ABOUT-02: Company values: Strategy First, Creative Intent, Long-term Growth - VERIFIED
- ABOUT-03: Two-column layout (story left, values right on desktop) - VERIFIED
- WHY-01: 4 differentiator cards - VERIFIED
- WHY-02: Differentiators: Strategy Before Execution, Data-Driven Decisions, Transparent Partnership, Agile & Adaptive - VERIFIED
- WHY-03: Each differentiator card shows icon, title, description, and stat - VERIFIED
</success_criteria>

<output>
After completion, create `.planning/phases/03-services-about/03-02-SUMMARY.md`
</output>
