---
phase: 01-foundation-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - irenic-media-new/package.json
  - irenic-media-new/tsconfig.json
  - irenic-media-new/tailwind.config.ts
  - irenic-media-new/app/layout.tsx
  - irenic-media-new/app/globals.css
  - irenic-media-new/app/page.tsx
  - irenic-media-new/lib/utils.ts
  - irenic-media-new/public/assets/images/marko-logo.png
  - irenic-media-new/public/assets/images/marko-logo-dark.png
  - irenic-media-new/public/assets/css/vendor/fontawesome.css
autonomous: true

must_haves:
  truths:
    - "Next.js 15 project runs without errors"
    - "Dark theme is applied by default on page load"
    - "Page displays without flash of wrong theme (FOUC)"
    - "Tailwind CSS utilities work with dark mode class toggle"
  artifacts:
    - path: "irenic-media-new/app/layout.tsx"
      provides: "Root layout with fonts, metadata, theme script"
      contains: "suppressHydrationWarning"
    - path: "irenic-media-new/app/globals.css"
      provides: "Marko template CSS variables and Tailwind imports"
      contains: "--accent-color: #C82AEF"
    - path: "irenic-media-new/tailwind.config.ts"
      provides: "Tailwind configuration with dark mode class"
      contains: "darkMode: 'class'"
  key_links:
    - from: "app/layout.tsx"
      to: "inline script"
      via: "dangerouslySetInnerHTML in head"
      pattern: "localStorage\\.getItem.*theme"
---

<objective>
Create a new Next.js 15 project with the marko template's visual foundation.

Purpose: Establish the project structure, CSS variables from marko template, and theme infrastructure that prevents hydration mismatches.

Output: A running Next.js app with dark theme default, marko CSS variables, and Tailwind configured for class-based dark mode.
</objective>

<execution_context>
@C:\Users\Raj Shah\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Raj Shah\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/raj/project/claude_tests/test_c/.planning/PROJECT.md
@D:/raj/project/claude_tests/test_c/.planning/ROADMAP.md
@D:/raj/project/claude_tests/test_c/.planning/phases/01-foundation-layout/01-RESEARCH.md

Reference codebases:
- marko_main_files/marko-react/src/assets/css/main.css — CSS variables and color scheme
- irenic-media/app/layout.tsx — Font setup pattern
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js 15 project with dependencies</name>
  <files>
    irenic-media-new/package.json
    irenic-media-new/tsconfig.json
  </files>
  <action>
Create a new Next.js 15 project in the `irenic-media-new` directory:

```bash
npx create-next-app@latest irenic-media-new --typescript --tailwind --app --no-eslint --no-src-dir
cd irenic-media-new
npm install framer-motion lucide-react clsx
```

The project should use:
- TypeScript with strict mode
- Tailwind CSS (configured during create-next-app)
- App Router (not Pages Router)
- No src directory (files directly in app/)

After creation, verify package.json has:
- next: ^15.x
- react: ^19.x or ^18.x
- framer-motion
- lucide-react
- clsx
  </action>
  <verify>
```bash
cd irenic-media-new && npm run dev
```
Server starts without errors on http://localhost:3000
  </verify>
  <done>Next.js 15 project created with framer-motion, lucide-react, clsx installed</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind and copy marko CSS foundation</name>
  <files>
    irenic-media-new/tailwind.config.ts
    irenic-media-new/app/globals.css
    irenic-media-new/lib/utils.ts
  </files>
  <action>
**tailwind.config.ts** — Configure for marko template compatibility:

```typescript
import type { Config } from "tailwindcss";

const config: Config = {
  darkMode: 'class',
  content: [
    "./pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./components/**/*.{js,ts,jsx,tsx,mdx}",
    "./app/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {
      colors: {
        accent: '#C82AEF',
        'accent-light': '#e3caff',
      },
      fontFamily: {
        sans: ['var(--font-jakarta)', 'sans-serif'],
      },
      borderRadius: {
        'marko': '25px',
      },
    },
  },
  plugins: [],
};

export default config;
```

**app/globals.css** — Extract CSS variables from marko template main.css:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

/* Marko Template CSS Variables */
:root {
  /* Dark theme (default) */
  --primary: #D1D1D1;
  --secondary: #040404;
  --text-color: #8B8B8B;
  --accent-color: #C82AEF;
  --accent-color-2: #FFFFFF;
  --accent-color-3: #1F1F1F;
  --accent-color-4: #0E0E0E;
  --accent-color-5: #0404047D;
  --accent-color-6: #C82AEF85;
  --accent-color-7: #FFFFFF8C;
  --global-border-radius: 25px;
}

/* Light theme */
.light {
  --primary: #1F1F1F;
  --secondary: #FFFAFA;
  --text-color: #4A4A4A;
  --accent-color: #C82AEF;
  --accent-color-2: #000000;
  --accent-color-3: #e3caff;
  --accent-color-4: #f5f5f5;
  --accent-color-5: #FFFFFF7D;
  --accent-color-6: #C82AEF33;
  --accent-color-7: #FFFFFF8C;
}

@layer base {
  html {
    scroll-behavior: smooth;
  }

  body {
    background-color: var(--secondary);
    color: var(--primary);
    font-family: var(--font-jakarta), sans-serif;
    transition: background-color 0.2s ease, color 0.2s ease;
  }
}

/* Utility classes matching marko template */
@layer components {
  .section {
    @apply py-24 px-5 md:py-32;
  }

  .hero-container {
    @apply max-w-[1280px] mx-auto w-full;
  }

  .accent-color {
    color: var(--accent-color);
  }

  .text-primary {
    color: var(--primary);
  }

  .text-secondary {
    color: var(--text-color);
  }

  .bg-secondary {
    background-color: var(--secondary);
  }
}
```

**lib/utils.ts** — Create cn() utility:

```typescript
import { clsx, type ClassValue } from 'clsx';

export function cn(...inputs: ClassValue[]) {
  return clsx(inputs);
}
```
  </action>
  <verify>
Create a test component that uses:
- `className={cn('text-primary', 'bg-secondary')}`
- Dark theme colors should be visible
  </verify>
  <done>Tailwind configured with darkMode: 'class', marko CSS variables in globals.css, cn() utility available</done>
</task>

<task type="auto">
  <name>Task 3: Set up layout.tsx with theme script and fonts</name>
  <files>
    irenic-media-new/app/layout.tsx
    irenic-media-new/app/page.tsx
    irenic-media-new/public/assets/images/marko-logo.png
    irenic-media-new/public/assets/images/marko-logo-dark.png
    irenic-media-new/public/assets/css/vendor/fontawesome.css
  </files>
  <action>
**Copy assets from marko template:**

```bash
# Create directories
mkdir -p irenic-media-new/public/assets/images
mkdir -p irenic-media-new/public/assets/css/vendor

# Copy logos
cp marko_main_files/marko-react/public/assets/images/marko-logo.png irenic-media-new/public/assets/images/
cp marko_main_files/marko-react/public/assets/images/marko-logo-dark.png irenic-media-new/public/assets/images/

# Copy Font Awesome CSS files
cp marko_main_files/marko-react/public/assets/css/vendor/fontawesome.css irenic-media-new/public/assets/css/vendor/
cp marko_main_files/marko-react/public/assets/css/vendor/fontawesome.min.css irenic-media-new/public/assets/css/vendor/
cp marko_main_files/marko-react/public/assets/css/vendor/brands.css irenic-media-new/public/assets/css/vendor/
cp marko_main_files/marko-react/public/assets/css/vendor/regular.css irenic-media-new/public/assets/css/vendor/
cp marko_main_files/marko-react/public/assets/css/vendor/solid.css irenic-media-new/public/assets/css/vendor/
```

**app/layout.tsx** — Root layout with theme script to prevent FOUC:

```typescript
import type { Metadata } from 'next'
import { Plus_Jakarta_Sans } from 'next/font/google'
import './globals.css'

const jakarta = Plus_Jakarta_Sans({
  subsets: ['latin'],
  variable: '--font-jakarta',
  display: 'swap',
})

export const metadata: Metadata = {
  title: 'Irenic Media | Strategy-Led Digital Marketing & Technology Agency',
  description: 'Growth without chaos. Strategy without noise. We are a digital marketing and technology agency focused on sustainable, long-term growth.',
  keywords: ['digital marketing', 'SEO', 'social media marketing', 'app development', 'web development', 'strategy'],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className={jakarta.variable} suppressHydrationWarning>
      <head>
        {/* Theme script runs before React hydration to prevent FOUC */}
        <script
          dangerouslySetInnerHTML={{
            __html: `
              (function() {
                const theme = localStorage.getItem('theme');
                // Default to dark theme if no preference stored
                if (theme === 'light') {
                  document.documentElement.classList.add('light');
                }
                // Dark is default, no class needed
              })();
            `,
          }}
        />
        {/* Font Awesome for icons matching marko template */}
        <link rel="stylesheet" href="/assets/css/vendor/fontawesome.css" />
        <link rel="stylesheet" href="/assets/css/vendor/brands.css" />
        <link rel="stylesheet" href="/assets/css/vendor/solid.css" />
      </head>
      <body className="font-sans antialiased">
        {children}
      </body>
    </html>
  )
}
```

**app/page.tsx** — Minimal page to verify setup:

```typescript
export default function Home() {
  return (
    <main className="min-h-screen bg-secondary">
      <div className="hero-container section">
        <h1 className="text-4xl font-bold text-primary">
          Irenic Media
        </h1>
        <p className="text-secondary mt-4">
          Growth without chaos. Strategy without noise.
        </p>
        <p className="accent-color mt-2">
          Accent color test: #C82AEF
        </p>
        <i className="fa-solid fa-check accent-color text-2xl mt-4 block"></i>
      </div>
    </main>
  )
}
```
  </action>
  <verify>
1. `npm run dev` — server starts
2. Visit http://localhost:3000
3. Page loads with dark background (#040404)
4. No flash of light theme before dark loads
5. Font Awesome check icon displays
6. Open DevTools > Application > Local Storage — set `theme: light`, refresh — page shows light theme
7. Programmatic check for theme script:
```bash
grep -c "localStorage.getItem('theme')" irenic-media-new/app/layout.tsx
```
Expected output: 1 (theme script exists in layout.tsx)
  </verify>
  <done>Layout.tsx has theme script preventing FOUC, Plus Jakarta Sans font loaded, Font Awesome CSS linked, dark theme default working</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Project runs:** `cd irenic-media-new && npm run dev` starts without errors
2. **Dark theme default:** Page loads with dark background (#040404), no FOUC
3. **Theme persistence:** Set localStorage `theme: light`, refresh shows light theme
4. **CSS variables work:** Accent color (#C82AEF) displays correctly
5. **Font Awesome works:** `<i class="fa-solid fa-check">` renders icon
6. **Fonts load:** Plus Jakarta Sans font visible (check DevTools Network tab)
</verification>

<success_criteria>
- [ ] irenic-media-new/ directory exists with valid Next.js 15 project
- [ ] `npm run dev` runs without errors
- [ ] Dark theme applied by default (no light flash)
- [ ] Theme toggleable via localStorage without hydration errors
- [ ] Marko template CSS variables available in globals.css
- [ ] Font Awesome icons render
- [ ] Plus Jakarta Sans font loads
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-layout/01-01-SUMMARY.md`
</output>
